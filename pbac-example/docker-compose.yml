version: '3.1'
services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "[>tRkC9SS+([l-x:ZX~Z"
      MYSQL_DATABASE: "db_pbac"
      MYSQL_USER: "pbac"
      MYSQL_PASSWORD: "123456"
    volumes:
    - ./mysql_data:/var/lib/mysql
    ports:
      - 3306:3306
