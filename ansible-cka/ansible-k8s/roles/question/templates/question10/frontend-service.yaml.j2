apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ question10_primary_namespace | default('frontend') }}
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
