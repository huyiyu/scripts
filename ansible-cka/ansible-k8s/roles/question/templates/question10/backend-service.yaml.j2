apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: {{ question10_secondary_namespace | default('backend') }}
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 80
