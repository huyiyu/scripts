# Question 1 | HPA autoscale
# 在 autoscale namespace 中创建一个名为 apache-server 的新 HorizontalPodAutoscaler(HPA)
# 要求:
# - 定位到 autoscale namespace 中名为 apache-server 的现有 Deployment
# - CPU 使用率目标 50%
# - 最小 1 个 Pod，最大 4 个 Pod
# - 缩小稳定窗口设置为 30 秒

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: apache-server
  namespace: autoscale
spec:
  # TODO: 填写 HPA 配置
  # 提示: 参考 https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/
